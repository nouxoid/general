<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="general.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:general.Views">
    <Page.Resources>
        <DataTemplate x:Key="ClipboardItemTemplate" x:DataType="local:ClipboardItem">
            <Grid ColumnSpacing="8" Padding="0,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Vertical" VerticalAlignment="Center" Grid.Column="0" Spacing="4">
                    <Button Width="32" Height="32" CornerRadius="6" ToolTipService.ToolTip="Copy" Click="OnCopyClicked">
                        <FontIcon Glyph="&#xE8C8;" FontSize="16" />
                    </Button>
                    <Button Width="32" Height="32" CornerRadius="6" ToolTipService.ToolTip="Paste" Click="OnPasteClicked">
                        <FontIcon Glyph="&#xE77F;" FontSize="16" />
                    </Button>
                </StackPanel>
                <TextBlock Grid.Column="1" TextWrapping="Wrap" VerticalAlignment="Center" Text="{x:Bind Content}" />
            </Grid>
        </DataTemplate>
    </Page.Resources>
    <StackPanel>
        <StackPanel x:Name="TopBar" Orientation="Horizontal" Spacing="8" Margin="8,8,8,0" />
        <TextBox x:Name="SearchBox" PlaceholderText="Search clipboard..." Margin="8,8,8,8" CornerRadius="6" TextChanged="OnSearchTextChanged" />
        <StackPanel x:Name="ActionBar" Orientation="Horizontal" Spacing="8" Margin="8,0,8,0" Visibility="Collapsed" />
        <ListView x:Name="ClipboardListView" Margin="8" CornerRadius="8" SelectionMode="Single" ItemTemplate="{StaticResource ClipboardItemTemplate}" SelectionChanged="OnListViewSelectionChanged" />
    </StackPanel>
</Page>
